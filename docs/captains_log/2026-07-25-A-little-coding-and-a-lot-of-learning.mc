MomentumDate 7, 25, 2026

Okay.

Day 3, and I made mistakes GALORE! LOL
The first part was good. I realized I had two versions of "App.tsx", one under src/components, the other directly under src. Cipher
(the name I gave my ChatGPT agent), again layed out why it was better to have App.tsx live in src instead of a component. Then, we
discussed Version Control. I made the point that all we did, today, was clean up the filesystem and the code without really changing
any functionality at all. So, I argued for build number 0.0.1a, a meaning a trivial clean up or reorganization that doesn't 
fundamentally change the operation of the project. This was agreed upon as a key disctinction of our project. Remember to ask Cipher
about how we should use the other places for versioning.

Then... PEBKAC and ID10T errors galore. Trying to run the project from VS Code, even though Vite so graciously just made me a direct
link to the running software. Then I realized the build number hadn't changed. Again, forgot to update the code with the build number.
(*SHEESH*)

Anyway, then we decided to commit, and I used the Git GUI within VS Code. Nice. Except it was stuck. Didn't realize VS Code opened a
nice editor for me to put my log notes in. Again, *sheesh*.

Anyway, all changes commited except this documentation. I'll be doing that next. Anyway, that's all for now!

'Til next time!